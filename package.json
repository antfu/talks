{
  "private": true,
  "scripts": {
    "build": "rimraf dist && pnpm -r run build"
  },
  "devDependencies": {
    "@iconify/json": "^2.0.25",
    "@slidev/cli": "^0.28.1",
    "@slidev/theme-default": "^0.21.1",
    "playwright-chromium": "^1.15.2",
    "pnpm": "^6.26.1",
    "rimraf": "^3.0.2"
  }
}
